{
  "build": {
    "builder": "nixpacks",
    "config": {
      "buildDependencies": [
        "composer"
      ],
      "startCommand": "npm run build && php artisan migrate --force && vendor/bin/heroku-php-apache2 public/"
    }
  }
}
